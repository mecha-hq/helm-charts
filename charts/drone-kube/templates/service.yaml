apiVersion: v1
kind: Service
metadata:
  name: {{ include "drone-kube.fullname" . }}
  labels:
    {{- include "drone-kube.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "drone-kube.selectorLabels" . | nindent 4 }}
